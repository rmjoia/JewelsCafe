<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="JewelsCafe.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:JewelsCafe.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title="Coffees"  Icon="coffees_icon.png">
            <ShellContent 
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>

        <Tab Title="Soft Drinks" Icon="soft_drinks_icon.png" >
            <ShellContent
                ContentTemplate="{DataTemplate local:SoftDrinksPage}"
                Route="SoftDrinksPage" />
        </Tab>

        <Tab Title="Food &amp; Snacks" Icon="snacks_icon.png" >
            <ShellContent
                ContentTemplate="{DataTemplate local:SnacksPage}"
                Route="SnacksPage"
                />
        </Tab>

        <Tab Title="Admin" Icon="admin_icon.png" >
            <ShellContent
                ContentTemplate="{DataTemplate local:AdminPage}"
                Route="AdminPage"
                />
        </Tab>
    </TabBar>

</Shell>

